const login = 'login';

const register = 'register';

const home = 'home';

const getCategory = 'categories';

const favourites = 'favorites';

const profile = 'profile';

const updateProf = 'update-profile';

const searchProd = 'products/search';

const myCarts = 'carts';

const details = 'products';
